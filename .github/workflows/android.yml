name: Build Android APK

on:
  push:
    branches: [main]
    paths:
      - 'android/**'
      - '.github/workflows/android.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK packages
        run: |
          yes | sdkmanager "platforms;android-34" "build-tools;34.0.0" "platform-tools" > /dev/null

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.6'

      - name: Build debug APK
        working-directory: android
        run: gradle assembleDebug --no-daemon --stacktrace

      - name: Stage APK
        run: |
          mkdir -p dist
          cp android/app/build/outputs/apk/debug/app-debug.apk dist/cast-helper.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: cast-helper-apk
          path: dist/cast-helper.apk

      - name: Publish APK to release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: app-latest
          name: 电视投屏 App(最新版)
          body: |
            在 Boox(安卓)上下载 `cast-helper.apk` 安装即可。

            用法:打开 App → 顶部输入你的视频网站 → 登录并播放 → 点「投屏到电视」。
            第一次先点右上角投屏图标连接电视;设备与电视需在同一 Wi-Fi。
          files: dist/cast-helper.apk
